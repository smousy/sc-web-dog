import { ScAddr } from "./ScAddr";
import { ScConstruction } from "./ScConstruction";
export declare const transformEdgeInfo: (construction: ScConstruction, aliasOrAddr: ScAddr | string) => void | {
    type: string;
    value: number;
};
